# یکدست‌سازی GUI صفحه‌های داخلی

مرجع: جدول‌ها و فرم‌های «تحویل و تحول دائم / موقت».

## محدوده

- قالب ثابت عنوان و بازگشت به خانه برای صفحه‌های داخلی؛ حفظ کنترل‌های اصلی و رویدادهای آن‌ها.
- فونت، رنگ، اندازه دکمه‌ها و ورودی‌ها، فاصله‌ها، جدول فشرده، فیلترها، انتخاب ردیف و صفحه‌بندی مشترک.
- نوار جست‌وجوی قابل‌مشاهده و حذف کنترل نمایشی تکراری جست‌وجو؛ دکمه ورود اکسل آبی‌خاکستری.
- فرم‌های دو ستونه در دسکتاپ و تک ستونه در موبایل؛ جلوگیری از افتادن منوی قدیمی روی صفحه‌های داخلی موبایل.
- داشبورد، تقویم، گفتگوها، استیکرها و تنظیمات از همین رنگ‌ها، ورودی‌ها و عنوان صفحه استفاده می‌کنند.
- کد خانه و کارت‌های آن، عنوان کارت‌ها، ترتیب و مقصد آن‌ها تغییر نکرده است.
- هیچ مهاجرت پایگاه داده، تغییر مجوز، ارسال پیام یا تغییر قواعد وضعیت وظایف در این اصلاح GUI انجام نشده است.

## بررسی

در مرورگر با دادهٔ ساختگی و قطع درخواست‌های خارجی، مسیرهای زیر باز شدند:

| گروه | صفحه‌ها |
| --- | --- |
| افراد | افراد و نقش‌ها، ورود و خروج، نشست‌های فعال |
| پیام | پیام‌ها، ارسال پیام، پیام‌های ارسال‌شده، پیگیری پاسخ، متن پیام‌ها، مدیریت استیکرها |
| گزارش | داشبورد، گزارش عملکرد، گزارش پاسخ‌ها، گزارش درخواست‌ها |
| تنظیمات | وضعیت‌ها و اولویت‌ها، تنظیمات هشدار، تنظیمات ایمیل، تنظیمات کاربری |
| وظایف | کانبان، آرشیو، تقویم و گانت، تأیید درخواست‌ها، سوابق درخواست‌ها، زنجیره تأیید |
| خودرو | تحویل دائم، تحویل موقت |
| گفتگو | چت گروهی، پیام مستقیم، گفتگو درباره وظیفه |

- نمایش دسکتاپ: ۱۳۶۳ پیکسل؛ مسیرهای بالا بدون بیرون‌زدگی افقی کل صفحه باز شدند. جدول‌های عریض اسکرول داخلی دارند.
- نمایش واکنش‌گرا: قاب ۳۹۰ پیکسلی مرورگر برای کانبان، تحویل دائم، تقویم، داشبورد و تنظیمات ایمیل. این بررسی جای آزمایش روی دستگاه فیزیکی را نمی‌گیرد.
- فرم‌های افزودن وظیفه و خودرو باز و بسته شدند؛ فرم خودرو در عرض ۳۹۰ پیکسل اسکرول داخلی دارد و دکمه‌های ثبت و انصراف در دسترس‌اند.
- آزمون‌های `npm test`: ۴۰ آزمون موفق، شامل حفظ DOM خانه، مقدار فرم و handlerها، بازترسیم دیرهنگام و مستثنا ماندن خوشامدگویی خانه.
- داده‌های ساختگی و مسیر آزمایش در `.ui-qa/` هستند و وارد Git یا نسخه انتشار نمی‌شوند.

## نکته نگهداری

چند ماژول قدیمی CSS با `!important` و شناسه‌های صفحه تولید می‌کنند. قرارداد جدید فقط به `.bamco-interior` و `.bamco-dialog` محدود است و اولویت کافی برای غلبه بر این قواعد دارد. CSS خانه همچنان آخر بارگذاری می‌شود. تابع `decorateView` کنترل‌ها را در همان DOM جابه‌جا می‌کند تا مقدار فرم و رویدادهای آن حفظ شوند.

## محدودیت

این بررسی سلامت GUI را پوشش می‌دهد. صحت ارسال ایمیل، ذخیره در پایگاه داده، تأیید درخواست‌ها و کنترل دسترسی سمت سرور باید در مرحلهٔ منطق و با حساب‌های مناسب جداگانه بررسی شود. این سند به‌معنی تکمیل همهٔ عملکردهای سامانه نیست.
