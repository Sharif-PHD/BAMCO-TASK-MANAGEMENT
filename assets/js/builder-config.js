/* BAMCO visual layout editor configuration.
 * Builder Content Public API Key is intentionally blank until a Builder Space is connected.
 * This key is public by design and may be committed to the repository.
 */
window.BAMCO_BUILDER_VISUAL = Object.freeze({
  apiKey: '',
  loginModel: 'bamco-login-layout',
  welcomeModel: 'bamco-welcome-layout'
});
