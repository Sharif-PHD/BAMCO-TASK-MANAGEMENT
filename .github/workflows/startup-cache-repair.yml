name: One-time topbar avatar cache repair

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/startup-cache-repair.yml'

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Force fresh topbar avatar script safely
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          old='assets/js/login-avatar-workload-fix-20260911.js?v=20260911-2'
          new='assets/js/login-avatar-workload-fix-20260911.js?v=20260911-3'
          if old in s:
              s=s.replace(old,new)
          elif new not in s:
              raise SystemExit('topbar avatar asset reference not found')
          p.write_text(s,encoding='utf-8')
          check=p.read_text(encoding='utf-8')
          assert '\ufffd' not in check
          assert new in check
          print('UTF-8 and topbar avatar cache bust: PASS')
          PY
      - name: Commit cache repair
        run: |
          if git diff --quiet -- index.html; then
            echo 'index already repaired'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'Force fresh coalesced topbar avatar fix'
          git push origin HEAD:main
