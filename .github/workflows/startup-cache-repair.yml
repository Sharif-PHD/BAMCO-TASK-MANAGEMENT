name: One-time table stability cache repair

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/startup-cache-repair.yml'

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Load pre-paint table fixes safely
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          pairs=[
            ('assets/js/table-pages.js?v=details-20260910-1','assets/js/table-pages.js?v=prepaint-20260911-1'),
            ('assets/js/table-suite.js?v=details-20260910-1','assets/js/table-suite.js?v=prepaint-20260911-1'),
          ]
          for old,new in pairs:
              if old in s: s=s.replace(old,new)
              elif new not in s: raise SystemExit('missing asset: '+old)
          p.write_text(s,encoding='utf-8')
          check=p.read_text(encoding='utf-8')
          assert '\ufffd' not in check
          for _,new in pairs: assert new in check
          print('UTF-8 and pre-paint table assets: PASS')
          PY
      - name: Commit cache repair
        run: |
          if git diff --quiet -- index.html; then
            echo 'index already repaired'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'Bump pre-paint table stability assets'
          git push origin HEAD:main
